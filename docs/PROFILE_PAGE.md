# صفحة الملف الشخصي (Profile Page)

## نظرة عامة

تم إضافة صفحة الملف الشخصي التي تسمح للمستخدم بعرض معلوماته الشخصية، تغيير كلمة المرور، ومعرفة صلاحياته.

## المميزات

### 1. معلومات المستخدم

- عرض اسم المستخدم
- عرض البريد الإلكتروني (إذا كان متوفراً)
- عرض الدور الوظيفي
- عرض تاريخ إنشاء الحساب

### 2. تغيير كلمة المرور

- إدخال كلمة المرور الحالية للتحقق
- إدخال كلمة المرور الجديدة
- تأكيد كلمة المرور الجديدة
- التحقق من صحة كلمة المرور (6 أحرف على الأقل)
- إظهار/إخفاء كلمات المرور

### 3. عرض الصلاحيات

- عرض جميع الصلاحيات الممنوحة للمستخدم
- تصنيف الصلاحيات بشكل واضح
- ترجمة الصلاحيات إلى اللغة العربية

## الوصول إلى الصفحة

### من القائمة العلوية

1. انقر على أيقونة المستخدم في الشريط العلوي
2. اختر "الملف الشخصي" من القائمة المنسدلة

### الرابط المباشر

```
/profile
```

## API Endpoints المستخدمة

### 1. GET /api/auth/profile

استرجاع بيانات المستخدم الحالي

**Response:**

```json
{
  "success": true,
  "data": {
    "id": 1,
    "username": "admin",
    "fullName": "المدير",
    "phone": "1234567890",
    "email": null,
    "role": {
      "id": 1,
      "name": "مدير"
    },
    "permissions": ["manage:*"],
    "createdAt": "2024-01-01T00:00:00.000Z"
  }
}
```

### 2. PUT /api/auth/change-password

تغيير كلمة مرور المستخدم

**Request:**

```json
{
  "currentPassword": "password123",
  "newPassword": "newPassword123"
}
```

**Response:**

```json
{
  "success": true,
  "message": "Password changed successfully"
}
```

## التحديثات على الملفات

### Frontend

#### 1. `packages/frontend/src/views/Profile.vue`

- صفحة جديدة للملف الشخصي
- عرض معلومات المستخدم
- نموذج تغيير كلمة المرور
- عرض الصلاحيات

#### 2. `packages/frontend/src/router/index.js`

- إضافة route للملف الشخصي: `/profile`
- لا يتطلب صلاحيات خاصة (متاح لجميع المستخدمين)

#### 3. `packages/frontend/src/layouts/MainLayout.vue`

- إضافة رابط "الملف الشخصي" في قائمة المستخدم
- تحديث عرض اسم المستخدم والدور

### Backend

#### 1. `packages/backend/src/routes/authRoutes.js`

- إضافة endpoint جديد: `PUT /auth/change-password`

#### 2. `packages/backend/src/controllers/authController.js`

- إضافة method `changePassword`

#### 3. `packages/backend/src/services/authService.js`

- إضافة دالة `changePassword` للتحقق من كلمة المرور الحالية وتحديثها
- تحديث `getProfile` لإرجاع بيانات الدور بشكل منظم
- تحديث `login` لإرجاع بيانات الدور بنفس الشكل

## الأمان

### التحقق من كلمة المرور

- يتم التحقق من كلمة المرور الحالية قبل تغييرها
- يتم تشفير كلمة المرور الجديدة باستخدام bcryptjs
- الحد الأدنى لطول كلمة المرور: 6 أحرف

### الحماية

- جميع endpoints محمية بـ JWT authentication
- لا يمكن للمستخدم تغيير كلمة مرور مستخدم آخر
- يتم التحقق من الهوية من خلال JWT token

## الترجمات

### أسماء الصلاحيات بالعربية

- `read:dashboard` → عرض لوحة التحكم
- `read:customers` → عرض العملاء
- `create:customers` → إضافة عملاء
- `update:customers` → تعديل العملاء
- `delete:customers` → حذف العملاء
- `manage:customers` → إدارة العملاء
- ... (وغيرها)

### ألوان الأدوار

- مدير → أحمر (error)
- محاسب → أزرق (primary)
- مبيعات → أخضر (success)
- مستخدم → رمادي (info)

## التجربة والاختبار

### 1. اختبار عرض الملف الشخصي

```bash
# في المتصفح
http://localhost:5173/profile
```

### 2. اختبار تغيير كلمة المرور

1. سجل الدخول كمستخدم
2. اذهب إلى الملف الشخصي
3. أدخل كلمة المرور الحالية
4. أدخل كلمة المرور الجديدة وأكدها
5. اضغط على "تغيير كلمة المرور"
6. تحقق من نجاح العملية
7. سجل الخروج وسجل الدخول بكلمة المرور الجديدة

### 3. اختبار الصلاحيات

- تحقق من عرض الصلاحيات الصحيحة للمستخدم
- تحقق من ترجمة الصلاحيات بشكل صحيح

## الملاحظات

1. **التوافق**: جميع التحديثات متوافقة مع النظام الحالي
2. **الأداء**: لا تأثير سلبي على الأداء
3. **الأمان**: تم تطبيق أفضل ممارسات الأمان
4. **UX**: واجهة سهلة الاستخدام ومتسقة مع بقية النظام

## المتطلبات

- Backend: Fastify + SQLite + Drizzle ORM
- Frontend: Vue 3 + Vuetify 3 + Vite
- Authentication: JWT + bcryptjs

## الصور التوضيحية

### معلومات المستخدم

```
┌─────────────────────────────────┐
│  👤 اسم المستخدم: admin        │
│  📧 البريد: غير محدد            │
│  🛡️  الدور: مدير               │
│  📅 تاريخ الإنشاء: 2024-01-01  │
└─────────────────────────────────┘
```

### تغيير كلمة المرور

```
┌─────────────────────────────────┐
│  🔒 كلمة المرور الحالية        │
│  [******************]           │
│                                 │
│  🔒+ كلمة المرور الجديدة       │
│  [******************]           │
│                                 │
│  ✅ تأكيد كلمة المرور           │
│  [******************]           │
│                                 │
│  [تغيير كلمة المرور]           │
└─────────────────────────────────┘
```

### الصلاحيات

```
┌─────────────────────────────────┐
│  ✓ عرض لوحة التحكم             │
│  ✓ إدارة العملاء               │
│  ✓ إدارة المنتجات              │
│  ✓ إدارة المبيعات              │
│  ✓ صلاحيات كاملة (مدير النظام) │
└─────────────────────────────────┘
```
