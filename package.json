{
  "name": "nuqtaplus",
  "version": "1.0.4",
  "description": "نقطة بلس - نظام إدارة نقاط البيع والمخازن",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "cross-env NODE_ENV=development concurrently \"pnpm --filter backend dev\" \"pnpm --filter frontend dev\"",
    "build": "pnpm --filter backend build && pnpm --filter frontend run publish:win",
    "lint": "pnpm -r lint",
    "format": "pnpm -r format",
    "seed": "node ./packages/backend/src/seed.js"
  },
  "keywords": [
    "accounting",
    "pos",
    "installment",
    "electron",
    "vue",
    "fastify"
  ],
  "author": "nuqtaplus Team",
  "license": "Proprietary",
  "devDependencies": {
    "concurrently": "^9.1.0",
    "cross-env": "^10.1.0",
    "eslint": "^9.15.0",
    "eslint-config-prettier": "^9.1.0",
    "eslint-plugin-vue": "^9.31.0",
    "fs-extra": "^11.3.2",
    "prettier": "^3.3.3"
  },
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=9.0.0"
  }
}
